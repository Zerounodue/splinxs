//- 
    To change this license header, choose License Headers in Project Properties.
    To change this template file, choose Tools | Templates
    and open the template in the editor.
     

extends layout

//use to append lines to block, not overwrite
//block append head
 
block content
  .container
    h1 #{title}
    p.lead __Say something worthwhile here.
    br
    if error
      br
      h2 #{error}
      br
    form(role='form', action="/login",method="post", style='max-width: 300px;')
      .form-group
          input.form-control(type='text', name="username", placeholder='#{texts.LOGIN.USERNAME}')
      .form-group
        input.form-control(type='password', name="password", placeholder='#{texts.LOGIN.PASSWORD}')
      button.btn.btn-default(type='submit') #{texts.GENERAL.SUBMIT}
      a(href='/')
        button.btn.btn-primary(type="button") #{texts.GENERAL.CANCEL}